This directory contains the edge surfaces (3D images with classification of each voxel) 
computed from the synthetic images in ../SyntImages by using classic Sobel filters 
(files with prefix "Sobel_") and our EK filters (files with prefix "EK_").
